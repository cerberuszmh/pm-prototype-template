---
description: Vue 弹窗尺寸、结构、操作区与新增编辑一致性约束
globs: "src/**/*Modal.vue"
alwaysApply: false
---

# Vue 弹窗约束

## 尺寸选择

- 必须根据内容数量、信息丰富度和交互复杂度选择最小且足够承载内容的规格。
- 多项条件同时命中时，按完全全屏 → 近全屏 → 标准尺寸的优先级选用较大规格。
- 尺寸在设计与实现阶段确定，弹窗打开后禁止根据实时内容切换尺寸。
- 标准尺寸适用于不超过 8 个字段、不超过 2 个分组且无复杂嵌套内容的单一业务对象，桌面参考尺寸为 `1200×600px`。
- 字段超过 8 个、分组达到 3 个或包含地图、表格、树、附件、主从结构、复杂联动及长内容时，必须使用近全屏。
- 完全全屏仅用于地图、画布、可视化编排或多面板工作台等必须占满视口的复杂操作。

## 框体结构

- 标准尺寸和近全屏弹窗必须保留四周安全边距；小视口下不得贴边或超出可视区域。
- Header、独立滚动的 Body 和固定 Footer 必须位于同一框体。
- Header 和 Footer 不得随正文滚动；内容溢出时只能由 Body 承担纵向滚动。
- Ant Design Vue 默认内边距不得导致标题与关闭按钮错位。
- 完全全屏弹窗占满视口、无圆角；使用自定义标题区时必须隐藏默认标题区，禁止两个标题同时出现。

## 底部操作区

- 所有取消、关闭、保存、创建等右侧操作必须放入 `.modal-footer__right`。
- 右侧第一个按钮为取消或关闭，最后一个按钮为主操作。
- 删除、作废等不可逆操作必须放入 `.modal-footer__left` 并使用危险样式。
- 无左侧危险操作时省略左区，由右区自身靠右；禁止依赖 `space-between` 处理单侧按钮。
- 条件显示的中间按钮直接作为右区平级元素，禁止额外使用 `a-space` 包裹。
- 弹窗底部危险按钮必须带对应语义图标；取消和关闭按钮不带图标。

## Props 与事件

- 打开状态通过 `open` prop 输入，通过 `update:open` 事件回传。
- 禁止在组件内部直接对 `open` prop 使用 `v-model`，禁止修改只读 prop。
- 保存或创建成功后使用 `success` 事件通知父组件，禁止由弹窗跨层修改父级列表状态。

## 新增与编辑一致性

- 同一业务实体的新增与编辑弹窗必须保持字段顺序、网格位置、样式和交互一致。
- 编辑弹窗允许比新增弹窗多出可编辑状态，但新增弹窗必须在相同位置用 Tag 展示系统默认状态。
- 系统自动确定的只读状态禁止使用只读输入框伪装成可编辑字段。
- 坐标等与地图强关联的输入必须放在地图所在区域，提示方位必须与实际位置一致。
- 双列布局的比例、卡片顺序、等高方式和辅助内容位置必须保持一致。
- 修改任一弹窗后，必须同步核查另一弹窗；未核查前不得声明完成。

## 验证

- 标准、近全屏和完全全屏规格至少各验证一次适用条件。
- 必须验证内容溢出、关闭、取消、校验失败、保存成功及小视口安全边距。
- 字段、状态、按钮和反馈文案必须与模块 SRS 一致。
