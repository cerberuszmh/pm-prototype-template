---
description: 原型 Mock 数据存放、响应式与发现约束
globs: "src/mock/**/*.{js,ts}"
alwaysApply: false
---

# Mock 数据约束

## 存放方式

- 单模块优先使用 `src/mock/{模块目录}.js`，文件名与页面模块目录保持一致。
- 跨页面或跨模块复用的枚举放入 `src/mock/enums.js`，禁止在多个模块重复定义同义枚举。
- 一个模块确需多个 Mock 文件时，才使用 `src/mock/{模块目录}/**/*.{js,ts}` 嵌套目录。
- 仅由单个 View 消费的简单数据允许内联，但必须使用可识别的具名 `reactive` 或 `ref` 变量。
- 跨页共享且需要增删改的数据必须以响应式对象导出，确保页面计算属性可感知变化。

## 数据语义

- 字段名、字段类型、枚举值和状态名称必须与模块 SRS 保持一致。
- 日期字段使用 SRS 约定格式；需要字符串排序时必须采用可直接比较的 ISO 格式。
- 多选字段使用字符串数组，禁止在 Mock 中降级为单个拼接字符串。
- 每条列表记录必须包含稳定唯一标识，禁止使用数组索引充当业务主键。
- Mock 只表达原型所需数据，不得擅自增加 SRS 未定义的业务状态或规则。

## Mock 组织与发现协议

评审、原型生成和开发交接必须按以下顺序发现 Mock：

1. 检查 `src/mock/{模块目录}.js`、`src/mock/{模块名}.js` 和 `src/mock/{模块目录}-*.js`。
2. 检查 `src/mock/{模块目录}/**/*.{js,ts}`。
3. 检查模块对 `src/mock/enums.js` 的 import，并只记录实际引用的 export。
4. 检查 `src/views/{模块目录}/**/*.vue` 中具名的内联 `reactive` 或 `ref` Mock。
5. 合并并去重全部命中项；只有四种来源均未命中时，才能判定“未找到 Mock”。

## 验证

- 至少核对一条正常数据、一条边界数据和一个终态数据。
- 新增记录后必须验证依赖该数据的 computed 或列表视图能够立即更新。
- 评审和开发交接不得把嵌套目录视为唯一 Mock 路径。
