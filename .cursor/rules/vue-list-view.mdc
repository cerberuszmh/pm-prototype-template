---
description: Vue 列表页、筛选、分页与批量操作约束
globs: "src/**/*ListView.vue"
alwaysApply: false
---

# Vue 列表页约束

## 页面结构

- 列表页使用 `.page-stack` 组织筛选卡与列表卡，纵向间距统一使用 `--page-section-gap`。
- 禁止使用 `a-space` 承担页面纵向布局，禁止在子卡片上叠加上下 margin。
- 筛选卡和列表卡不显示重复标题；页面标题由标签页与面包屑承担。
- 列表卡操作按钮放入 `#extra`；仅有 `#extra` 时允许卡头左侧留白。
- 含 Card Header 的列表卡 Body 必须保持四周 `10px` 的实测视觉间距；顶部需补偿 Header 与 Body 的 `1px` 边框重叠。
- 页面必须覆盖空态、异常态和禁用态，不得只交付正常数据状态。

## 筛选区

- 查询与重置按钮必须始终可见。
- 展开/收起按钮与查询/重置同行并位于其左侧，使用链接式按钮；文字在前、方向图标在后。
- 筛选卡内边距由全局样式统一维护，禁止页面 scoped 样式重复覆盖。
- 重置必须同时恢复筛选值、展开状态及相关分页状态，避免界面状态与查询结果不一致。

## 表格与排序

- 表格统一使用 `size="middle"`。
- 默认按数据创建时间倒序展示；用户手动排序只覆盖当前会话。
- 日期字符串只有在采用可直接比较的 ISO 格式时才允许使用字符串排序，否则必须转换为时间值。
- 可变状态列使用可点击状态 Tag 与下拉菜单，并排除当前状态。
- 停用、删除、归档等危险状态变化必须二次确认；普通状态变化可直接执行。
- 可点击业务单元格必须使用统一链接视觉，默认色与 hover 色遵循 Ant Design Vue 主色令牌，禁止自行扩展同义色。
- 列表操作列的链接按钮禁止添加图标。

## 分页

- 默认每页 `10` 条，可选项固定为 `10 / 20 / 30 / 50`。
- 必须显示总数、每页条数切换和快速跳页；框架因总页数不足隐藏快速跳页时不视为缺陷。
- 特殊业务场景可调整总数单位，但不得改变分页结构和选项。

## 多选与批量操作

- 必须通过受控 `rowSelection` 和稳定唯一的 `row-key` 管理选择状态。
- 不允许参与批量操作的终态记录必须禁用 Checkbox；处理函数仍须再次排除终态。
- 无选中项时批量按钮必须禁用，有选中项时才允许执行。
- 批量主操作由下拉按钮左侧直接执行；下拉菜单不得重复主操作。
- 删除、归档等危险批量操作必须二次确认。
- 批量完成后必须清空选择状态，使 Checkbox 和按钮恢复初始状态。
- 选中至少一条时，分页区域左侧必须显示「取消选择」；点击后只清空选择，不修改业务数据。
- 窄屏下必须验证「取消选择」与可见分页项不重叠。

## 验证

- 至少验证：无数据、单页、多页、终态禁选、批量成功后清空、危险操作取消和窄屏分页。
- 状态名称、按钮名称和反馈文案必须与模块 SRS 一致。
