---
description: 会话切换习惯与上下文成本控制
globs: ""
alwaysApply: true
---

# 会话切换与上下文成本控制

## 背景

Cursor 单次会话的上下文 token 随对话轮次累积上升，长时间不切换会话会出现：

- **成本爆炸**：每次 AI 回复都重新读取整条历史，token 消耗与轮次成平方级增长。
- **检索退化**：早期关键决策与近期讨论混在一起，AI 容易在"应当遵守的硬约束"和"已撤销的过时讨论"之间混淆。
- **回写延迟**：大量原型小改积累为"未回写"，跨会话上下文丢失，下次开会话时连"上次改了什么"都不记得。

本规则提供**可机器执行的会话切换触发条件**，避免凭感觉判断。

## 强制触发新会话的场景（命中任一即新开会话）

| # | 触发条件 | 新会话开场动作 |
|---|---|---|
| 1 | 用户调用 `/评审` SKILL（含 /严格审查、/原型评审 任一阶段） | 让用户主动新开一个会话，AI 在新会话中先读 `docs/_待回写清单.md`、最近一轮严格审查报告（如有）、目标模块 PRD/SRS，再开始执行评审 |
| 2 | 用户调用 `/交付检查` SKILL | 新会话；先读交付物 + `docs/_待回写清单.md` 后再执行 |
| 3 | 用户调用 `/开发交接` SKILL | 新会话；先读 PRD / SRS / 原型 / 评审结论 + `docs/_待回写清单.md` 后再生成数据字典等开发包 |
| 4 | 用户调用 `/需求变更` SKILL | 新会话；先读 PRD / SRS / 变更涉及代码 + `docs/_待回写清单.md` 后再写变更说明 |
| 5 | 模块阶段切换（如 M4 收尾切到 M5） | 新会话；先读项目级 PRD §11 模块编号 + §9 决策表与新模块所属 P05 节点 |
| 6 | 单次会话累计**已修改 ≥ 8 个文件**（含 .vue / .md / .mdc / mock 数据） | 用户在下次有"完整功能交付"语义的请求时（如"再做下一个功能""换一个模块"），AI 主动建议新开会话并给出"上下文已较深"的提示 |
| 7 | 单次会话历经 ≥ 30 轮对话且话题已切换 | AI 主动提示「本会话已较长，建议切换新会话，从清单/PRD 重新读取上下文」，但**不强制**——由用户决定 |
| 8 | 大型规则 / SKILL 体系改造完成后（如本次合并 SKILL + 合并 P07/P08 等结构性变更） | 强制新开会话，让新规则在新上下文中按"全新约束"生效 |

## AI 主动建议新开会话的措辞模板

满足触发条件 6 / 7 / 8 时，AI 应在当轮回复末尾追加：

```
---
**会话切换建议**

本会话已 {trigger 描述，如「修改了 12 个文件，上下文较深」}，建议新开一个会话继续，理由：
- {成本理由：token 累积接近 X，新会话单轮成本将下降 ~Y%}
- {检索理由：本会话早期讨论与当前任务相关性已较低，新会话能让 AI 聚焦}
- {回写理由：已积累 N 条 RB-XXX 待回写条目，新会话可在 §第一步统一回写}

新会话开场建议输入：「{推荐的开场 prompt}」
```

## 新会话开场的标准读取清单（强制）

任何新会话的第一轮，AI **必须**先用 `Read` 工具按顺序读取下列上下文文件，再开始执行用户请求：

| 顺序 | 文件 | 是否强制 |
|---|---|---|
| 1 | `docs/_待回写清单.md` | ✅ 强制（即使为空也要读，确认无遗留） |
| 2 | 用户请求中明确提及的目标文档（PRD / SRS / 原型 / 评审报告等） | ✅ 强制 |
| 3 | `docs/formal-requirements/P07-{系统名}产品需求文档（PRD）.md` 的 §9 已确认决策表 + §10 术语口径 + §11 模块编号 | ⚪ 视任务而定（涉及术语 / 决策 / 模块编号时强制） |
| 4 | 与本次任务相关的 SKILL（按 `项目总入口` SKILL 分流） | ✅ 强制 |
| 5 | 涉及代码改动时，读取相关 `.vue` / `.js` / mock 数据 | ✅ 强制 |

> **禁止**：在新会话首轮跳过上述清单直接执行用户请求；如确实因用户输入已含完整上下文可绕过，须在回复开头显式声明"已检视用户输入足以覆盖标准清单，跳过 §X / §Y 读取"。

## 与其他规则的关系

- 本规则不覆盖 `项目总入口` SKILL 的分流逻辑——分流仍按任务类型走，本规则仅约束"在哪个会话里执行"。
- 本规则不覆盖 `project-collaboration.mdc §"原型修改回写策略"` 的回写时机——回写时机仍按"触发窗口"判定，本规则只是把"触发窗口"作为新会话的入口标记。
- 本规则与 `file-edit-safety.mdc` 互不影响——文件编码安全在任何会话中都强制执行。

## 用户侧操作建议

| 场景 | 建议操作 |
|---|---|
| 一个完整功能模块（PRD → SRS → 原型 → 评审）完工 | 切换新会话进入下一模块 |
| 一次"评审 + 修复 + 复核"闭环完成 | 切换新会话进入下一轮迭代 |
| 一次"原型大改"或"重构"完成 | 切换新会话，让 AI 在新上下文中按"完工后"状态继续 |
| 一次"答疑 / 闲聊 / 探索"产生 ≥ 20 轮无落盘对话 | 切换新会话，把有价值的决策先固化到对应 md / 规则文件中 |
