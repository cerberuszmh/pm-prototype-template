---
description: 答复忠实执行核查通用规则——所有用户决策驱动后续动作的 SKILL 必须引用本规则
globs: ".cursor/skills/**/*.md"
alwaysApply: false
---

# 答复忠实执行核查（Answer Faithfulness Check）

本规则定义全项目通用的"用户答复忠实执行核查"流程。**所有"用户决策驱动后续动作"的 SKILL 都必须在自身流程末尾显式引用本规则**，避免"修复建议覆盖用户答复"的隐性偏差跨 SKILL 复现。

## 适用 SKILL

下列 SKILL 在用户给出决策后须强制执行本规则核查：

| SKILL | 触发场景 |
|-------|---------|
| `评审` 阶段 1（严格审查） | 用户对 P1/P2/P3/TBD 问题给出"答复"后 |
| `评审` 阶段 2（原型评审） | 用户对 §5.1 阶段决策表 6 项填写后 |
| `需求变更` | 用户对变更说明的"确认执行 / 不确认 / 部分确认"决策后 |
| `文档生成与版本管理` | 用户对升版决策、跨文档引用同步范围确认后 |
| `接口对齐` | 用户对接口对齐方案、字段归属决策后 |
| `交付检查` | 用户对修改建议的接受/拒绝决策后 |
| `开发交接` | 用户对 §第四步 A/B/C 选项及"按需增项 6 项"勾选答复后（仅适用步骤 3~5：解析方向 / 核查改动 / 偏差报告；不涉及报告回写） |

未列入但属于"用户决策驱动后续动作"的 SKILL，须主动遵循本规则。

## 强制流程

每轮修复全部完成、声明"已完成"之前，**必须**执行以下流程：

### 步骤 1：读取最新一轮报告

用 `Read` 工具读取本轮产出的报告文件全文（如严格审查报告、原型评审结论、变更说明等）。

### 步骤 2：逐项提取答复

对每个待决策问题（含 P1/P2/P3/TBD/FIX/Q/CR 等所有编号），提取"答复："冒号后的实际内容（去掉空白行、引导语后的纯指令文本）。

### 步骤 3：解析答复方向

判断答复属于以下哪一类，并以**答复方向为最高优先级**：

| 答复关键词 | 含义 | 执行方向 |
|------|------|---------|
| "按建议修复" / "按修复建议执行" | 完全采纳"修复建议" | 执行修复建议正文 |
| "按 XX 文案" / "以 XX 为准" / "保留 XX" | **覆盖**修复建议，按答复内容执行 | 以答复字面要求为准，**禁止**回退到"修复建议" |
| "方案 A / B / C" | 选择修复建议给出的具体方案 | 执行该方案 |
| "不修复" / "推迟" | 本轮不动 | 跳过 |
| 业务侧补充信息（如"× 字段为准"） | 含字段名、术语、值的具体定义 | 按答复定义同步代码与文档 |
| 空白 | 用户未答复 | **不得自行启动修复**，提示用户填写 |

### 步骤 4：逐项核查实际改动

对每条非空答复，回读对应代码/文档片段，确认改动方向与答复方向一致：

- 答复"按 X 文案"，须确认 `Grep` 结果显示文案确实是 X，不是修复建议的同义词
- 答复"方案 B"，须确认实际执行的是方案 B 的所有动作（不是方案 A）
- 答复"以 X 为准"，须确认 PRD/SRS/代码三方均统一为 X

### 步骤 5：偏差报告

任一项发现答复方向与实际改动方向不一致，**立即停止"修复完成"声明**，在对话中明确指出：

```
⚠️ 答复忠实执行偏差：
- 问题编号：X
- 用户答复方向：[原答复字面内容]
- 实际执行方向：[实际改动内容]
- 偏差性质：[修复建议覆盖了答复 / 答复未被解析 / 部分执行]
- 处理选项：① 按答复重新执行；② 用户重新决策方向
```

等用户裁决后再继续。

### 步骤 6：核查通过后追加标识

在执行报告末尾追加一行：

```
答复忠实执行核查：通过（共核查 N 项答复，全部与实际改动方向一致）
```

### 步骤 7：状态回写（强制，缺一不可）

核查通过后，**必须**用 `StrReplace` 工具回写报告对应表格中每条问题的"状态"列，将原始性质（如"待修复 / 新发现 / 遗留 / 待业务确认"）更新为明确的最终状态：

| 实际处理结果 | 回写状态 |
|------|------|
| 按建议或答复方向执行修复完毕 | `✅ 已修复（简要说明，如"按方案 A"或"按建议"）` |
| 业务方确认并落地（含"追认现状"） | `✅ 已闭环（业务确认 / 决策记录）` |
| 用户决策"不修复 / 推迟" | `🟡 已推迟（备注下次重启条件）` |
| 用户决策"不接受 / 不修复" | `❌ 不修复（决策依据）` |

- **回写格式建议**：`原始性质 → ✅ 最终状态（简要说明）`，保留历史性质便于追溯。
- **同步更新报告头部"总体评级"**（如最初为"存在主要问题"且本轮全部修复，应追加一行"修复后复核：所有问题已闭环（YYYY-MM-DD）"）。

## 禁止事项

- **禁止**在用户答复明确指向 X 时，仍执行修复建议中的 Y。
- **禁止**用"修复建议更优"、"用户答复有歧义"、"代码与文档已自洽"等理由覆盖答复方向，如有歧义须**主动追问**用户确认。
- **禁止**将"答复忠实执行核查"写成走过场——必须逐项 `Read`/`Grep` 验证，不得凭记忆。
- **禁止**修复完成后保留"待修复 / 新发现 / 遗留 / 待业务确认 / 待确认"等中间态作为最终状态。修复完成（含"决策不修复"）即视为闭环动作完成，必须按步骤 7 回写明确终态（✅ / 🟡 / ❌）。
- **禁止**修复多个问题后只回写部分；本轮修复影响的所有行均须同步更新，不得遗漏。

## SKILL 引用方式

各 SKILL 在自身决策核查小节中加入以下引用即可，不必重复定义流程：

```markdown
### 第 N 步：用户决策后的执行核查（强制）

按 `.cursor/rules/answer-faithfulness.mdc` 全流程执行：
- 读取本轮产出报告 → 提取答复 → 解析方向 → 核查改动 → 偏差报告 → 追加核查标识 → 状态回写。
- 任一步骤违规，立即停止"修复完成"声明并报告给用户。
```

## 设计动机

实战中常出现"用户答复明确指向 X，但实际改动落到了修复建议的 Y"的偏差，且因为代码与文档内部自洽，多轮审查仍可能未察觉。本规则即为防止该类隐性偏差跨任意 SKILL 复现而设计。
