---
description: Vue + Ant Design Vue 原型的通用实现约束与场景规则入口
globs: src/**/*.vue
alwaysApply: false
---

# Vue 原型通用约束

## 适用范围与权威源

- 本文件只保留所有 Vue 页面都必须遵守的通用约束，并作为场景规则入口。
- 字段、控件、校验、状态和异常反馈以模块 SRS 为权威源；Vue 实现必须与其一致。
- 页面结构和交互实现同时受对应场景规则约束，禁止只读取本文件后忽略场景规则。
- 实现示例与选择器细节见 `docs/guides/Vue界面实现参考.md`；示例不得覆盖规则或 SRS。

## 场景规则索引

- 工作台、侧边栏、登录入口和标签页：`.cursor/rules/vue-layout-navigation.mdc`。
- 列表、筛选、分页和批量操作：`.cursor/rules/vue-list-view.mdc`。
- 弹窗尺寸、结构和新增/编辑一致性：`.cursor/rules/vue-modal.mdc`。
- Mock 存放、数据语义和发现顺序：`.cursor/rules/mock-data.mdc`。
- 若页面文件名未命中场景规则，但实际包含对应场景，必须主动读取该规则，禁止以 glob 未命中为由跳过。

## 技术基准

- 统一使用 `Vue 3 + Vite + Ant Design Vue 4.2.6`，禁止退回静态 HTML。
- 优先使用官方组件、属性和事件；只有官方能力无法满足需求时才允许增加自定义实现。
- 组件名和路由名必须贴近业务语义；路由页使用 `View` 后缀，弹窗组件使用 `Modal` 后缀。
- 页面必须覆盖空态、异常态和禁用态，不得只交付正常态。
- 禁止添加 SRS 未定义的业务字段、状态、权限或校验规则。

## 页面与组件通用约束

- 页面名称、字段名称、状态名称和按钮名称必须与 PRD、SRS、菜单和路由元数据保持一致。
- UI 中引用页面或标签页名称必须使用直角引号「」，禁止使用 ASCII 双引号或英文“Tab”。
- HTML 属性外层使用双引号时，属性值内部禁止再次使用 ASCII 双引号，避免 Vue 模板编译失败。
- 修改共享组件、全局样式或跨页状态后，必须检查所有已知消费方，禁止只验证当前页面。
- `<script setup>` 中修改 ref 必须通过具名函数完成，禁止在模板内联箭头函数中写 `ref.value = ...`。

## 表单控件选型规范

- 日期使用 `a-date-picker` 并声明与 SRS 一致的 `value-format`；日期时间场景增加 `show-time`。
- 数字、金额和面积使用 `a-input-number`；整数增加 `precision="0"`。
- 枚举单选使用 `a-select`，枚举多选使用 `a-select mode="multiple"`，多选数据模型必须是字符串数组。
- 长文本使用 `a-textarea`；手机号使用限制长度的 `a-input`。
- 系统自动确定的只读状态使用 Tag 和说明文字，禁止使用只读输入框伪装成可编辑字段。
- 不确定字段类型时先核对 SRS，禁止为追求实现方便统一降级为 `a-input`。

## 表单校验失败的统一行为

- 所有视觉上带星号且实际必填的字段必须同时声明 `name` 和 `rules`，禁止只使用 `required` 展示星号。
- 调用 `validate()` 失败时必须同时做到：字段级错误提示、`message.warning("请完善必填项后再操作")`、滚动到第一个错误字段。
- 自动滚动使用平滑滚动并把错误字段置于视口中部，避免被固定标题区遮挡。
- 字段总数不超过 3 个且始终完整显示时可以省略滚动，但不得省略字段错误和 Toast。
- 条件必填优先使用自定义 validator；确因动态字段或跨区域联动无法接入 rules 时，必须在界面、业务校验和 SRS 中同时说明条件。

## Toast 文案规范

- 保存成功统一使用 `保存成功`。
- 普通提交成功使用 `XX 已提交`，状态切换成功使用 `已切换为 XX 状态`。
- 表单校验失败统一使用 `请完善必填项后再操作`，禁止按保存、提交等动作改写同义文案。
- 删除成功使用 `XX 已删除`；具体对象名称必须与 SRS 一致。
- 原型占位按钮只反馈可评审的成功结果，如 `导出成功`；禁止展示“开发中”“暂未支持”“请稍候”等开发状态。
- 占位区域使用“「XX」内容即将上线，敬请期待”等未来兑现型文案，禁止暴露内部开发进度。
- 禁止使用 `console.log`、`console.warn` 或 `console.error` 代替用户可见反馈。
- Toast 精确文案必须与 SRS §5.x.5 和对应 AC 一致；PRD 只保存业务反馈语义，不承担精确技术文案。

## 按钮与危险操作

- 取消和关闭按钮使用默认样式且不带图标。
- 列表操作列的链接按钮不带图标。
- 删除使用 `DeleteOutlined`，作废使用 `StopOutlined`。
- 删除、作废、停用、归档等不可逆或高影响操作必须二次确认。
- 操作按钮文案以动作语义清晰为准，禁止同一动作混用多个同义名称。

## Mock 组织与发现协议

- Mock 的完整权威规则已迁移到 `.cursor/rules/mock-data.mdc`，评审、原型生成和开发交接必须同时读取该文件。
- 发现顺序固定为：扁平模块文件 → 模块嵌套目录 → 实际引用的共享枚举 → View 内具名响应式 Mock。
- 只有四种来源均未命中时，才能判定“未找到 Mock”。
- 禁止把嵌套目录视为唯一 Mock 路径，禁止遗漏内联数据或共享枚举。
- 双速轨草稿 Mock 与正式隔离：草稿模块的 Mock 放 `src/mock/_draft/{模块目录}.js` 或页面内联；评审与开发交接的发现范围**排除** `_draft`（草稿不得作为评审或交接依据）。

## 草稿与正式原型目录约定（双速轨）

- 双速轨快速轨的草稿页面只放 `src/views/_draft/{模块目录}/`；正式页面放 `src/views/{模块目录}/`，靠目录区分，组件名不加 Draft 后缀。
- 草稿路由必须带 `meta: { draft: true, module: 'M{n}' }`，可集中挂在 `/draft/...`；菜单归入独立「草稿预览」分组，避免客户把草稿误当正式页面。
- 草稿只实现轻量需求摘要中 PM 已批的字段/规则；未批项用占位或隐藏并注释 `待PM确认`，禁止做成好像已定业务规则的强校验。
- 晋升（方向确认 + 正式轨 SRS 定稿后）：移动页面到正式目录 → 去掉 draft meta → 删除 `_draft` 副本 → 更新路由/菜单；禁止长期双份维护。
- 评审、开发交接等正式场景扫描 `src/views/` 时排除 `_draft/`；仅存在 `_draft` 的模块视为「原型未产出」。

## 复核分级

- 每次写入后先回读修改区域，确认无乱码、引号成对、标签完整。
- 文案、字号、颜色和静态间距：回读代码并确认一次页面。
- 布局和响应式：桌面端与窄屏端各验证一次。
- 表单、批量操作和路由：验证主流程与关键异常路径。
- 大范围重构、模块切换或正式交付：完整检查显隐、触发、相对位置、间距、文案、状态切换和反馈。
- 未完成对应层级复核前，不得声明已完成、已还原或可验收。
