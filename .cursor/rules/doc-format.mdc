---
description: Markdown 文档格式、命名与流程图规范
globs: "**/*.md"
alwaysApply: false
---

# 文档格式规范

## Markdown 输出
- 输出可直接归档的正式文档，不写聊天式答案。
- 使用清晰标题层级与表格，减少无结构大段文本；列表项用短句，同章节语气粒度一致。
- 正式文档包含：文档信息、版本记录、正文、风险依赖、验收标准。
- 引用路径、文件名、术语使用统一命名，不临时改叫法。

## 状态符号规范（强制）

> **背景**：项目内所有 SKILL、规则、报告、数据字典使用的状态/检查点符号必须遵循以下统一约定，避免视觉混乱与同义符号并存。

### §1 终态符号（用于报告"状态"列、问题汇总表、最终结论）

| 符号 | 写法（含 Unicode） | 语义 | 适用场景 |
|---|---|---|---|
| ✅ | U+2705 | 已修复 / 已闭环 / 已确认 / 通过 | 严格审查 / 交付检查 / 接口对齐报告的"状态"列；评审决策列 |
| 🟡 | U+1F7E1 | 已推迟（暂时搁置，未来可重启） | 同上；搭配"备注下次重启条件" |
| ❌ | U+274C | 不修复 / 业务方拒绝 / 失败 | 同上 |

### §2 流程检查点符号（用于 SKILL 输出末尾、阶段性输出）

| 符号 | 写法（含 Unicode） | 语义 | 适用场景 |
|---|---|---|---|
| 🛑 | U+1F6D1 | 已停止（强制检查点，等用户确认下一步） | SKILL 五步流程末尾；阶段性输出末尾 |

### §3 中间态/警告符号（**特殊：必须带 VS16 变体选择符**）

| 符号 | 写法（含 Unicode） | 语义 | 适用场景 |
|---|---|---|---|
| ⚠️ | U+26A0 **+ U+FE0F**（VS16） | 待业务确认 / 警告 / 注意 | 待确认字段标注；试运行表"关键发现"列；任何"业务方需补充答复"的提示 |

> **强制要求**：⚠ 是双面字符（U+26A0），单独写作 `⚠` 时多数字体按"文本风格"渲染为**黑白线条三角**，看不出彩色。**必须**在其后追加 VS16 变体选择符 U+FE0F，写作 `⚠️`，才能强制按彩色 emoji 渲染（黄底黑边三角）。
>
> 同类双面字符（如 ☎/☎️、❄/❄️、♥/♥️、✈/✈️、☀/☀️）若在文档中使用，同样必须带 VS16。

### §4 错误写法黑名单（禁止再次出现）

| 错误写法 | 应改为 |
|---|---|
| `⏸ 已推迟` | `🟡 已推迟` |
| `⏸ 已停止` | `🛑 已停止` |
| `⏸ 请...`（用作检查点） | `🛑 请...` |
| 裸 `⚠`（不带 VS16） | `⚠️`（带 VS16） |

### §5 自检方法

- **裸 ⚠ 残留检查**（在 `.cursor/` `templates/` `docs/` 范围内执行）：

  ```powershell
  # 注意：本规范文件 doc-format.mdc 自身因需展示反面示例，必然包含裸 ⚠ 与裸 ⏸ 字面字符，应在扫描中排除
  $hits = Get-ChildItem -Path "<root>\.cursor","<root>\templates","<root>\docs" -Recurse -File -Include *.md,*.mdc `
    | Where-Object { $_.FullName -notlike '*doc-format.mdc' } `
    | Select-String -Pattern ([char]0x26A0) -SimpleMatch -Encoding UTF8
  $bare = 0
  foreach ($h in $hits) {
    $idx = 0
    while (($idx = $h.Line.IndexOf([char]0x26A0, $idx)) -ge 0) {
      $next = if ($idx + 1 -lt $h.Line.Length) { [int]$h.Line[$idx + 1] } else { -1 }
      if ($next -ne 0xFE0F) { $bare++ }
      $idx++
    }
  }
  "裸 ⚠ 残留: $bare 处（应为 0）"
  ```

- **⏸ 残留检查**：直接 `Grep` 搜 `⏸`（同样排除 `doc-format.mdc` 与历史归档报告），应返回 0 处。

- 文档生成 / 升版 / 报告归档前必须执行上述自检。

> **规范豁免说明**：本规范文件 `doc-format.mdc` 自身在 §3 / §4 / §5 含若干"裸 ⚠"和"⏸"字面字符（用作反面示例与教学说明），属于必要保留。所有自检脚本必须排除本文件自身。

## 正式交付文档命名（强制中文 + 分类编号前缀）

### 编号前缀规则（强制）

> **背景**：Cursor 资源管理器按文件名字母序排序，不加前缀无法体现文档类别与前后关系。本节统一 P/M 建设文档与 A 验收文档的分类编号约定，便于在资源管理器中识别文档定位和顺序。

| 前缀模式 | 适用范围 | 编号分配时机 | 编号长度 |
|---|---|---|---|
| `P{nn}-` | **项目级文档**（系统级一份，全项目唯一） | 由本规则一次性固定（见下方"项目级编号分配表"） | 双位数（P01、P02...P99） |
| `M{n}-{nn}-` | **模块级文档**（每模块一组） | `{n}` 在 P05 功能模块清单或 P07 §11 模块编号固定表中统一分配；`{nn}` 由本规则一次性固定（见下方"模块级编号分配表"） | 双位数（M1-01、M2-04...） |
| `A{nn}-` | **项目验收专用文档**（项目级派生文档） | 仅在用户通过验收专用手动命令显式触发时生成 | 双位数（A01、A02、A03） |

**项目级编号分配表（强制，不得改动）**：

| 前缀 | 文档类型 | 阶段 |
|---|---|---|
| `P01-` | 素材提炼与价值取舍 | 产品发现 §3.0 |
| `P02-` | 需求地图 | 产品发现 §3.1 |
| `P03-` | JTBD（用户任务）分析 | 产品发现 §3.2 |
| `P04-` | 用户旅程图 | 产品发现 §3.3 |
| `P05-` | 功能模块清单 | 产品发现 §3.4 |
| `P06-` | 信息架构（IA） | 产品发现 §3.5 |
| `P07-` | 项目级 PRD（**合并版**，承担原"项目对齐稿"+"项目级 PRD"职责） | 项目启动 + 项目级 PRD |
| `P08-` | **已废弃**（原项目级 PRD 已合并入 P07，新项目不再使用此编号） | — |

**模块级编号分配表（强制，不得改动）**：

| 前缀 | 文档类型 | 阶段 |
|---|---|---|
| `M{n}-01-` | 结构化需求底稿（**已废弃，仅老模块兼容**） / 老系统盘点与翻新决策（翻新模式专用，仍产出） | 模块启动 |
| `M{n}-02-` | 模块级 PRD（**二件套核心入口**，新模块嵌入 §需求背景 + §待确认清单 替代 M{n}-01 底稿） | 模块 PRD |
| `M{n}-03-` | 模块级 SRS | 模块 SRS |
| `M{n}-04-` | 流程说明 | 模块流程 |

> **{n} 分配原则**：产品发现 P05 确定模块清单后，在 P07 §11 模块编号固定表中按业务优先级或菜单顺序依次分配 M1、M2、M3...；一旦写入 P07 即固定，后续新增模块往后追加，不得插队回填。
>
> **模块级二件套（强制）**：新模块默认采用「M{n}-02 PRD + M{n}-03 SRS」二件套，**不再单独产出 M{n}-01 结构化需求底稿**。已存在的 M{n}-01 底稿文件（如 M4-01、M7-01）保留不动作为历史交付物；翻新模块仍按"老系统盘点与翻新决策"产出 M{n}-01。详见 `项目与模块启动 SKILL §2.1 末「模块级二件套」段` 与 §2.4 阶段二 PRD 输出内容。

**项目验收专用编号分配表（强制，不得改动）**：

| 前缀 | 文档类型 | 定位 |
|---|---|---|
| `A01-` | 需求规格说明书 | 汇编验收基线内的项目级需求与验收标准 |
| `A02-` | 概要设计说明书 | 描述验收基线对应软件的总体技术设计 |
| `A03-` | 详细设计说明书 | 按模块汇编验收基线对应软件的详细实现设计 |

> A 系列仅用于项目验收材料，不参与 P/M 建设文档编号，不加入项目建设推荐流程，也不作为建设阶段权威源。

### 命名格式表（强制）

| 文档类型 | 命名格式 |
|---------|---------|
| 素材提炼与价值取舍（产品发现，系统级） | `P01-{系统名}素材提炼与价值取舍.md` |
| 需求地图（产品发现，系统级） | `P02-{系统名}需求地图.md` |
| JTBD 用户任务分析（产品发现，系统级） | `P03-{系统名}JTBD（用户任务）分析.md` |
| 用户旅程图（产品发现，系统级） | `P04-{系统名}用户旅程图.md` |
| 功能模块清单（产品发现，系统级) | `P05-{系统名}功能模块清单.md` |
| 信息架构 IA（产品发现，系统级） | `P06-{系统名}信息架构（IA）.md` |
| 项目级 PRD（**合并版**，承担原"对齐稿 + 项目级 PRD"双重职责；新项目直接走此单一文件） | `P07-{系统名}产品需求文档（PRD）.md` |
| 模块级结构化需求底稿（**已废弃，仅老模块兼容**；新模块嵌入 PRD §需求背景） | `M{n}-01-{模块名}模块结构化需求底稿.md` |
| 老系统盘点与翻新决策（翻新模式专用，仍产出） | `M{n}-01-{模块名}模块老系统盘点与翻新决策.md` |
| 模块级 PRD（**二件套核心入口**，新模块强制含 §需求背景 + §待确认清单） | `M{n}-02-{模块名}模块产品需求文档（PRD）.md` |
| 模块级 SRS | `M{n}-03-{模块名}模块软件需求规格说明书（SRS）.md` |
| 流程说明 | `M{n}-04-{模块名}模块流程说明.md` |
| 原型评审结论（模块级） | `{模块名}原型评审结论.md` |
| 原型评审结论（项目/阶段级） | `原型评审结论与阶段决策-R{轮次}-{YYYYMMDD}.md`（如 `原型评审结论与阶段决策-R1-20260413.md`） |
| 严格审查报告 | `{模块名}严格审查报告-R{轮次}-{YYYYMMDD}.md`（如 `用户管理严格审查报告-R1-20260101.md`，每轮递增；修复复核不新增轮次） |
| 需求变更说明（单次） | `{模块名}需求变更说明.md` |
| 需求变更记录（追加式） | `{模块名}变更记录.md`（单文件维护本模块所有 CR-XXX 条目） |
| 接口对齐报告 | `{模块名}接口对齐报告.md` |
| 交付检查报告 | `{模块名}交付检查报告-{YYYYMMDD}.md`（**仅在用户明确要求归档时生成**，默认作为对话输出） |
| 测试用例 | `{模块名}测试用例.md` |
| 数据字典（开发交接专用） | `{模块名}数据字典.md` |
| 待确认问题清单 | `{模块名}待确认问题清单.md` |
| 用户手册 / 使用说明书 | `{模块名}用户手册.md` / `{模块名}使用说明书.md` |
| 项目验收需求规格说明书 | `A01-{项目名}需求规格说明书.md` |
| 项目验收概要设计说明书 | `A02-{项目名}概要设计说明书.md` |
| 项目验收详细设计说明书 | `A03-{项目名}详细设计说明书.md` |

- `{模块名}` 必须替换为实际中文业务名称（如"项目档案管理"），禁止英文缩写。
- **模块级文档命名后缀强制带"模块"二字**（如 `M4-01-活动管理模块结构化需求底稿.md`），与项目级文档（P 系列）形成视觉区分；文档内"文档名称"字段同步带"模块"二字。
- `{系统名}` 为产品发现系统级交付物专用前缀（取自全局系统名称），整个系统各一份，不按模块拆分。
- 括号使用全角 `（）`，多模块项目中模块名在前、文档类型在后。
- **报告类文档（评审 / 审查 / 变更 / 接口对齐 / 数据字典 / 测试用例 / 手册等）不加 P/M 前缀**——这类文档是事后产物或贯穿多轮的支撑材料，加前缀反而干扰排序。
- **文档内"文档名称"字段须与文件名前缀保持一致**（如 `《P02-艾伯维报名系统需求地图》`），跨文档引用其他文档时同样带前缀。
- **轮次型报告（严格审查 / 项目级原型评审）**：同一模块第一次为 R1，后续完整重审递增；修复后的补充验证不新增轮次，在原报告中追加"修复复核"节。
- **A 系列验收文档**：`{项目名}` 使用项目完整名称；三份文档均为项目级单文件，文档内按 P07 §11 的模块编号组织内容。文档信息必须注明“项目验收专用”和“派生文档，非建设权威源”。

## PRD / SRS 分工边界（强制，单一权威源原则）

> **背景**：实战中常见 PRD 与 SRS 出现 50% 以上内容重叠（字段表、AC、非功能需求三处最严重）。冗余维护会造成"PRD 改了 SRS 没改/反之"的同步漏洞。本规则明确两份文档的职责分工，确立**单一权威源**原则，根治此类问题。

### 分工原则

| 维度 | PRD（产品需求文档） | SRS（软件需求规格说明书） |
|------|------------------|------------------------|
| **视角** | 业务视角："做什么 + 为什么 + 给谁用" | 技术视角："怎么实现 + 字段细节 + 可验证" |
| **目标读者** | 产品负责人 / 业务负责人 / 评审参与方 | 前端开发 / 后端开发 / 测试 / 联调对接方 |
| **核心问题** | 业务诉求是否被完整表达 | 实现规格是否具体到可开发可测试 |

### 章节归属表（强制）

| 内容类型 | 唯一权威源 | 另一份文档处理 |
|---------|----------|--------------|
| 业务背景 / 项目目标 / 用户痛点 | **PRD** §3~§5 | SRS 不重复，须用时引用 PRD §3~§5 |
| 角色与用户 / 业务场景 | **PRD** §7 | SRS 不重复，须用时引用 PRD §7 |
| 业务流程概述（主流程/异常流程） | **PRD** §8 | SRS 不重复，仅在 §5.x.3 处理逻辑中具体化技术细节 |
| 待确认清单（待确认业务问题 Q-XXX） | **PRD** §13 | SRS 不维护业务待确认问题清单 |
| **字段清单 / 控件类型 / 校验规则** | **SRS** §5.x.2 | PRD 不写字段表，仅在 §9 功能需求明细中按"业务功能描述"列字段名+用途，不展开类型/校验/约束 |
| **接口与数据要求** | **SRS** §6 | PRD 不涉及 |
| **AC 验收标准** | **SRS** §10 | PRD §12 不重复列出 SRS-AC，仅列业务级"是否达成业务目标"的高层验收（如"用户能完成项目档案查询全流程"） |
| **状态机 / 业务规则细节** | **SRS** §5.x.4 | PRD §9.x.3 仅写业务侧规则简述，不展开技术实现细节 |
| **异常处理详细处置** | **SRS** §5.x.5 | PRD §8.4 仅写业务异常场景描述，不展开 Toast 文案/字段下方提示等技术细节 |
| **非功能需求详细指标** | **SRS** §8 | PRD §10 仅写业务侧 SLO（如"99% 用户 5 秒内能找到项目"），不写技术侧响应时间等指标 |

### 引用语法（强制）

PRD 中如需引用 SRS 内容，统一使用：

```markdown
> **字段详细规格**：见 SRS §5.3.2 字段清单
> **完整 AC 验收标准**：见 SRS §10
```

SRS 中如需引用 PRD 内容，统一使用：

```markdown
> **业务背景**：见 PRD §3 编写目的
> **角色定义**：见 PRD §7 角色与用户
```

引用必须含具体章节编号，**禁止**仅写"详见 SRS"或"详见 PRD"无章节定位。

### 强制约束

- **PRD 不得包含字段类型定义**（如"`a-input`"、"最多 100 字"等技术规格表述应仅出现在 SRS）
- **PRD 不得列出与 SRS 完全对应的 AC 编号清单**（PRD 验收标准应是业务级、非技术级）
- **SRS 不得展开业务背景、用户痛点、项目目标**（这些只在 PRD 维护）
- 同一信息**只在一处定义**，另一份文档需要时引用，不复制
- 文档生成或升版时，须按本规则核查归属，否则不得声明完成

### 项目级 PRD vs 模块级 PRD 分工（强制）

> 一个项目可同时存在两份 PRD：项目级与模块级，二者职责边界明确，禁止互相重复。

| 维度 | 项目级 PRD | 模块级 PRD |
|---|---|---|
| **文件命名** | `{项目全称}产品需求文档（PRD）.md` | `{模块名}产品需求文档（PRD）.md` |
| **文档内首行标题** | `# {项目全称} 产品需求文档（PRD · 项目级）` | `# {模块名} 产品需求文档（PRD · 模块级）` |
| **粒度** | 系统级，全项目一份 | 模块级，每个功能模块一份 |
| **承担内容** | §1 项目背景与目标（引用产品发现成果）<br>§2 业务目标与角色<br>§3 模块全景索引（链接到各模块 PRD，回填）<br>§4 模块间依赖矩阵（回填）<br>§5 项目级权限模型<br>§6 项目级非功能需求（性能 / 安全 / 兼容 / 合规）<br>§7 项目级验收标准与里程碑<br>§8 风险与依赖（项目级）<br>§9 已确认决策表　§10 术语口径　§11 模块编号　§12 本期范围（对齐决策段） | 该模块的业务背景、角色权限、功能范围、交互说明、业务规则、模块级验收标准 |
| **禁止内容** | 不得展开模块业务功能细节；不得重复产品发现 6 件套已写的需求地图 / JTBD / 旅程 / 功能模块 / IA 详情，只引用 | 不得重复项目级背景、非功能需求、权限模型、验收里程碑，只引用项目级 PRD |

> **模板锚点（强制）**：项目级 PRD 须以 `templates/requirements/project-prd-template.md` 为起点，其章节编号（§1 项目背景与目标 …… §9~§12 对齐决策段）以该模板为**唯一权威**；模块级 PRD 仍以 `templates/requirements/module-prd-template.md` 为起点。二者编号体系相互独立，禁止混用。

### 项目级 PRD 两阶段节奏（强制）

项目级 PRD 不是一次性写完，分两阶段：

| 阶段 | 时机 | 写入章节 | 留空章节 |
|---|---|---|---|
| **骨架版** | 合并版项目级 PRD 对齐决策段（§9~§12）完成后、首个模块级 PRD 之前 | §1 / §2 / §5 / §6 / §7 / §8 / §9 / §10 / §11 / §12 | §3 留空模块全景索引；§4 留空依赖矩阵 |
| **滚动回填** | 每个模块 PRD 完成后 | 把该模块加入 §3 索引；把暴露的依赖加入 §4 矩阵 | — |
| **封板版** | 全部模块完工、交付检查之前 | §3 / §4 全部填完；复核 §1 / §5 / §6 / §7 / §8 / §9 是否需更新 | — |

> 跳过判定：骨架版要求 §1 / §2 / §5 / §6 / §7 / §8 / §9~§12 非空（§3 / §4 留空待回填）；封板版要求 §3 / §4 完整。

### 重叠率上限（强制）

- PRD/SRS 同模块的章节内容重叠率（按行数估算）须 **≤ 15%**
- 重叠率 > 15% 视为违规，须在严格审查中标记为冗余问题
- `评审` SKILL 阶段 1 §第三步"文档层扫描"的"冗余检查"维度强制执行此判定

### SRS 粒度说明（强制，避免命名疑惑）

> **背景**：SRS（软件需求规格说明书）一词源自 IEEE Std 830，原意为"一个软件一份"。本项目采用**模块级 SRS** 实践（每个功能模块一份 `{模块名}软件需求规格说明书（SRS）.md`），与命名出处的"软件级"语义存在表面不一致。本节统一口径，避免新人疑惑或误造"项目级 SRS"造成体系冗余。

#### 为什么采用模块级 SRS

中大型项目下，软件级 SRS（一份巨型文档承载全系统）实际不可维护——多人并行冲突、模块独立发布困难、严格审查粒度过粗。现代敏捷/模块化交付主流采用模块级 SRS，本项目沿用此实践。

#### 软件级 SRS 应承担的内容，本项目分布到何处（强制对应）

| 软件级 SRS 通常承担 | 本项目落点 |
|---|---|
| 非功能需求技术规格（性能 / 兼容 / 安全 / 合规） | **项目级 PRD §6** |
| 系统级权限模型 | **项目级 PRD §5** |
| 系统级验收标准与里程碑 | **项目级 PRD §7** |
| 跨模块字段命名标准 / 术语统一 | **项目级 PRD §10 术语口径** + **数据字典** |
| 系统级集成接口规格 | **接口对齐报告** |

#### 强制约束

- **禁止新建"项目级 SRS"**：上述软件级锚点内容已分布承担，再造一份会与项目级 PRD §5/§6/§7/§10 + 数据字典高度重叠，违反单一权威源原则。
- **验收专用例外**：用户通过验收专用手动命令显式触发时，可生成 `A01-{项目名}需求规格说明书.md`。该文件是验收派生汇编文档，不得命名为“项目级 SRS”，不得放入 `docs/formal-requirements/`，不得成为需求权威源，也不得反向覆盖 P07、模块级 PRD 或模块级 SRS。
- **模块级 SRS 不得展开项目级非功能需求 / 权限模型 / 里程碑**，仅引用项目级 PRD 对应章节（语法见 §"PRD/SRS 分工边界" 引用语法）。
- **模块级 SRS 命名统一为 `M{n}-03-{模块名}模块软件需求规格说明书（SRS）.md`**，不重命名为"功能规格说明书""模块规格说明书"等，保持与开发 / 测试 / QA 行业通用术语一致。

## 核心需求文档版本字段统一规范

项目级 PRD、模块级 PRD、模块级 SRS 的文档信息表统一使用两个独立字段：

- `所属项目`：只填写项目完整名称，不拼接版本号。
- `文档版本`：只填写当前版本号，格式为 `V{主版本}.{次版本}`。

`文档版本` 必须与版本记录表最新一条的版本号一致。禁止继续使用 `所属项目/版本` 合并字段，避免升版工具解析同一单元格中的混合信息。

## SRS 文档统一规范

新建 SRS 时必须以 `templates/requirements/srs-template.md` 为起点，不得从空白文件开始，也不得照搬其他已有 SRS。

**§1 文档信息表字段顺序（强制）：**

| 字段 | 顺序 |
|------|------|
| 文档名称 | 第 1 行 |
| 所属项目 | 第 2 行 |
| 文档版本 | 第 3 行 |
| 作者 | 第 4 行 |
| 评审人 | 第 5 行 |
| 当前状态 | 第 6 行 |
| 创建日期 | 第 7 行 |
| 最近更新日期 | 第 8 行 |
| 对应 PRD | 第 9 行（最后） |

**项目全称（强制）：** `所属项目` 必须写完整全称（全称由本项目首次启动时确定，写入本规则替换下方占位）：  
`{项目全称占位 · 启动新项目时替换}`

**章节格式（强制）：**
- §3～§9 各章节字段名使用 `**加粗**`（如 `**涉及系统**`）
- 章节之间、功能子节之间使用 `---` 分隔线
- §6 接口说明开头使用 `>` 引用块说明当前阶段为原型
- §11 附录使用 `### 子标题` + 表格结构（术语说明用表格，参考文档用列表）

**§5 功能规格子节结构（强制）：** 每个功能点均使用 §5.x.1～§5.x.5 五段式：  
功能描述 → 页面与字段 → 处理逻辑 → 状态与规则 → 异常处理

**§2 版本记录规范（强制）：**

- 条目总数控制在 **3～5 条**，超出时须合并碎片版本。
- **合并规则**：同日内多个碎片修复版本可归并为一条，版本号取该组最高版本，修改说明以 `合并 Vx.x–Vy.y：` 为前缀。
- **合并说明内容**：只写功能级关键词（如"批量打印交互变更""删除功能规格新增"），**不写**内部审查编号（如 `P1-001`、`P3-005`）。
- **禁止合并**：不同日期且代表独立功能里程碑的版本不得合并。
- **版本号连贯性**：合并后跳过的中间版本号无需补全，但须在合并前缀中标明合并范围（如 `合并 V0.4–V0.8`）。

**§2 5 条临界自动合并检查清单（强制，每次升版前执行）：**

任何文档版本升级（如新增 V0.X 条目）前，必须执行以下 4 步检查，避免违规：

1. **计数当前条目**：用 `Grep` 或 `Read` 工具统计 `§2 版本记录` 表格的数据行数（不含表头）。
2. **预估升级后条目数**：升级后 = 当前条目数 + 1。若 ≤ 5，无须合并；若 > 5，须先合并再升级。
3. **执行合并（条目数将超过 5 时强制）**：
   - 选取最早的连续若干条（通常是 V0.1～V0.X-1，留下最近的关键里程碑版本）合并为一条。
   - 合并条目格式：`合并 V0.1–V0.X：[功能级关键词，2~4 个]`。
   - 合并后的日期取被合并条目中最晚的那个日期。
   - 修改人取最早被合并条目的修改人（或写"—"）。
4. **合并后再升版**：保证总条目数 ≤ 5（推荐 ≤ 3），格式如：

   | 版本 | 日期 | 修改人 | 修改说明 |
   |---|---|---|---|
   | 合并 V0.1–V0.4 | YYYY-MM-DD | — | [合并范围内的功能级关键词，2~4 个] |
   | V0.5 | YYYY-MM-DD | — | [前一次升级内容] |
   | V0.6 | YYYY-MM-DD | — | [本次升级内容] |

> **触发示例**：当前 5 条 V0.1～V0.5，要升级 V0.6 时，先合并 V0.1～V0.4 为一条 `合并 V0.1–V0.4：…`，结果保留 3 条（合并条目 + V0.5 + V0.6），合规。

## 交付物存储路径

正式交付文档统一存放在 `docs/` 下对应子目录，目录不存在时按需创建。

| 交付物类型 | 存储目录 | 状态 |
|---|---|---|
| PRD | `docs/formal-requirements/` | 已存在 |
| SRS | `docs/formal-requirements/` | 已存在 |
| 需求变更说明 / 老系统盘点与翻新决策 / 历史兼容底稿与对齐稿 | `docs/formal-requirements/` | 已存在 |
| 原型评审结论 / 评审问题清单 / 严格审查报告 / 交付检查报告 | `docs/review/` | 已存在 |
| 需求变更记录 | `docs/changes/` | 按需创建 |
| 用户原始需求材料 / 待确认问题清单 | `docs/user-requirements/` | 已存在 |
| 业务流程图 / 流程说明 | `docs/flow/` | 按需创建 |
| 测试用例 | `docs/testing/` | 按需创建 |
| 数据字典 | `docs/data/` | 按需创建 |
| 使用说明书 / 用户手册 | `docs/manuals/` | 按需创建 |
| 接口对齐报告 | `docs/api-alignment/` | 按需创建 |
| 产品发现 6 件套（素材提炼 / 需求地图 / JTBD / 用户旅程图 / 功能模块清单 / 信息架构 IA） | `docs/discovery/` | 按需创建 |
| 项目验收文档（A01～A03） | `docs/acceptance/` | 仅显式触发时按需创建 |

## 非交付支撑文件（保留英文名）
`templates/`、`docs/user-requirements/raw/` 下的文件，以及 `SKILL.md`、`README.md` 等约定文件。

`docs/samples/` 为**非正式交付的脱敏成品样例目录**：目录内文件可沿用正式中文命名以演示成品形态，但不得计入真实项目交付物，也不得直接改名后对外提交。

## 流程图
- 先写目标、触发条件、参与角色，再给 Mermaid 图，图后补充异常与边界说明。
- 优先 `flowchart TD`；节点用业务动作命名（"提交申请""主管审批"）；一节点一动作。
- 判断节点须明确条件；异常流必须覆盖失败、驳回、超时、权限不足等场景。
- 图中角色/状态名称与需求文档和原型保持一致。
