---
description: Vue 工作台布局、导航、标签页与登录入口约束
globs: "src/{layouts/**/*.vue,components/PageTabs.vue,views/LoginView.vue}"
alwaysApply: false
---

# Vue 布局与导航约束

## 适用与权威源

- 本规则只约束工作台外壳、顶部导航、侧边栏、标签页、登录入口及路由页固定操作区。
- 具体 CSS 与组件参考见 `docs/guides/Vue界面实现参考.md`；发生冲突时以本规则的可验证结果为准。
- 页面名称、菜单名称和角色名称必须与项目级 PRD、模块 SRS 及路由元数据一致。

## 全局布局

- Header 与侧边栏品牌区高度必须统一为 `50px`。
- 页面纵向分区间距必须使用 `--page-section-gap`；禁止子卡片再用上下 margin 叠加同义间距。
- 内容区左右内边距统一为 `20px`；窄屏可按既有响应式规则收紧。
- 页面标题由激活标签页与面包屑共同承担，禁止恢复独立标题区造成重复。
- 桌面端侧边栏必须提供稳定可达的展开/收起入口；切换后菜单、背景与内容区同步适配，禁止出现不可点击空白带。
- 品牌区展开时显示系统图标和系统名称；收起时隐藏名称并保留居中图标，禁止文字截断后残留。

## 滚动与响应式

- 桌面端工作台必须锁定为单视口布局，禁止由 `body` 承担工作台滚动。
- 左侧菜单与右侧内容必须独立滚动；任一区域滚动不得改变另一区域的 `scrollTop`。
- 侧边栏必须保留滚动能力但隐藏滚动条。
- 在 `max-width: 992px` 下必须恢复自然页面滚动，禁止固定高度造成内容裁切。
- 验证时必须分别滚动左右区域，并在桌面端与窄屏端确认全部内容可访问。

## 顶部导航与标签页

- Header、标签栏和面包屑必须由同一个 `.workspace-topbar` 连续包裹并整体吸顶；禁止分别固定并手工计算偏移。
- `PageTabs` 必须同时承载标签栏和面包屑，二者共享白底和唯一底部分隔线。
- 面包屑数据顺序固定为：系统首页 → `route.meta.parentTitle`（如有）→ `route.meta.title`。
- 标签页只能通过菜单或路由打开；系统首页常驻且不可关闭。
- 标签页 key 优先使用 `route.meta.menuKey`，缺省时使用 `route.path`。
- 关闭当前标签页时按右邻 → 左邻 → 系统首页选择后继页面；关闭非激活页不得触发导航。
- 刷新当前标签页必须通过组件重新挂载实现，并明确会清空组件内部状态。
- 右键菜单和三点菜单必须复用同一组关闭与刷新能力；菜单关闭、路由变化和组件卸载时必须清理浮层及监听器。
- 标签页状态只在当前会话保存，禁止写入 `localStorage`。

## 顶部用户操作

- 头像只显示颜色圆圈，不在头像内部添加图标。
- 顶部直接退出按钮与用户菜单中的退出项必须复用同一处理函数和反馈文案。
- 退出登录属于高影响操作，所有入口必须先使用同一二次确认；确认后反馈成功并跳转登录页。

## 登录入口

- 登录页必须作为顶层路由挂载，禁止嵌入 `WorkspaceLayout`。
- 登录容器必须限制在单视口内，禁止产生无业务意义的 body 滚动条。
- 必须使用 Ant Design Vue 表单；用户名和密码接入真实必填校验。
- 验证码若仅用于原型展示，必须明确不参与校验，禁止制造“看似必填但不校验”的假规则。
- 动画必须响应 `prefers-reduced-motion`，减少动态效果时自动停用。

## 路由页固定操作区

- 新建或详情路由页使用固定底部操作栏时，页面根容器必须预留底部空间。
- 桌面端操作栏不得覆盖侧边栏；窄屏时必须收紧左右边距并验证不遮挡正文。
- 操作区遵循左侧返回、右侧主操作的稳定位置关系。
