---
description: 项目协作流程与文档写作通用规范
alwaysApply: true
---

# 项目协作与文档规范

## 协作流程
- 新项目默认先做需求清洗与产品发现，不直接写 PRD / SRS / 原型。
- 项目级第一轮输出产品发现成果；全新模块第一份正式文档直接为模块级 PRD，其中嵌入需求背景、角色概述与待确认清单，不再单独生成结构化需求底稿。
- 未完成范围边界与关键问题对齐前，不生成完整正式文档或整套原型。
- 项目级 PRD 的 §9~§12 对齐决策段是范围、术语和模块编号的权威源；模块 PRD 建立在该决策段与产品发现成果之上。
- SRS、流程图、原型、测试用例沿用同一套术语、角色、状态和字段名。
- 原型完成后进入 `评审` SKILL；评审通过后由用户确认是否进入开发交接，开发交接产出即为 PM 责任终点。
- 评审通过后将关键交互口径沉淀为规则，避免迭代回退。

**推荐输出顺序**：项目级产品发现 → **合并版项目级 PRD（P07）**（先填对齐决策段 §9~§12，再补骨架章节）→ 模块级按所选档位推进：

- **档位 A · 文档先行（保守默认）**：模块级 PRD（含 §需求背景 + §待确认清单，二件套首件）→ 模块级 SRS（二件套次件）→ 流程图 → 原型 → 评审【阶段 1 严格审查 → 阶段 2 原型评审】→ 开发交接（PM 责任终点）。
- **档位 B · 双速轨（客户要先看页）**：轻量需求摘要（M{n}-00，PM 逐条批决策）→ 原型草稿（`src/views/_draft/`）→ 客户反馈迭代 → 方向确认闸门 → 反向定稿 PRD → SRS（标准不降）→ 正式原型对齐 → 评审【同两阶段】→ 开发交接（PM 责任终点）。

两档的评审与开发交接门槛完全一致；草稿原型未经正式轨与评审不得进入开发交接。档位选择标准与切换规则见 `项目与模块启动` SKILL §1.4。

## 双速轨协作约定（模块级档位 B）

- **何时启用**：客户要先看可点页面、业务细节未齐但页面清单可谈时，模块级可选双速轨档；选择标准与 🛑 确认节点见 `项目与模块启动` SKILL §1.4。
- **工作件性质**：轻量需求摘要（`docs/working/M{n}-00-…`）是快速轨唯一正式工作文档，**非 PRD/SRS、非交付物**，不得进入开发交接强制五项。
- **决策权在 PM**：字段/规则候选由 AI 起草为「建议」，PM 决策列默认空或「待定」；AI 不得擅自锁定关键字段、状态机或业务规则。
- **草稿与正式隔离**：草稿原型只放 `src/views/_draft/` 并带 draft 标识；正式原型在 `src/views/`。草稿未经方向确认 + 正式轨 + 评审两阶段，不得作为交付或评审依据。
- **快速不降正式**：正式轨 SRS 的字段/控件/校验/状态/技术 AC 标准与文档先行档完全一致。

## 项目验收文档独立边界

- A01 需求规格说明书、A02 概要设计说明书、A03 详细设计说明书是项目验收专用派生文档，不属于上述项目建设推荐输出顺序。
- A 系列文档不作为 PRD、模块级 SRS、原型评审或开发交接的前置条件，也不替代任何建设阶段交付物。
- A 系列文档只读取并汇编既有资料，不反向修改建设文档与代码；如需修订权威来源，须由用户另行触发对应建设流程。
- A 系列文档不计入 PM 交付物或 PM 开发交接包，不延长 PM 责任边界；也不得作为程序员设计、开发、联调、构建或实现工作的依据。
- 资料不足时可生成标注“⚠️ 待补充”的草稿，但不得把待补充内容视为已确认事实或正式验收结论。

> **技术隔离权威源**：手动触发、第零步校验、二次确认、跨技能禁止调用、评审/交付检查排除与变更回归约束，统一见 `.cursor/rules/acceptance-skill-isolation.mdc`。本节只保留业务边界，不重复定义技术隔离细节。

## 文档写作
- 使用正式、准确、可评审的中文；术语、页面名、状态名、字段名全局一致。
- 避免"优化一下""支持相关能力"等不可验证表达；不确定信息统一标注"待确认"，不编造事实。
- 需求描述须补齐业务规则、边界条件、异常处理和验收标准；结论、风险、依赖显式成节。
- 正式交付文档（PRD、SRS、用户手册等）文件名使用中文，命名规则见 `doc-format.mdc`。

## 输出自检
- 是否说明为什么做、做什么、不做什么，给出可验证的验收标准。
- 是否标出依赖项、风险项与待确认问题。
- 页面交互类需求：控件显隐规则、按钮触发方式与失败兜底、布局位置关系与间距是否明确。

## 签字门槛执行约定（Cursor 执行专用）

**背景**：项目正式文档（项目对齐稿 / PRD / SRS 等）的"签字"字段，仅用于**真实世界打印 / 政策合规流程**，不是 Cursor 协作中的推进门槛。

**执行约定**：
- AI 在 Cursor 中推进流程时，**不需要**追问"业务方是否签字" / "PRD 是否签字" / "对齐稿是否签字"等问题。
- AI **不得**以"签字未完成"为由阻止下一阶段推进（包括 PRD → SRS → 原型 → 严格审查 → 原型评审等任意阶段）。
- AI **不得**主动建议业务方"重新签字 V1.x"等动作；用户在对话中口头确认 / 答复即视为有效推进依据。
- 文档内已有的"签字栏 / 签字状态 / 已签字"措辞保留，作为真实世界打印态记录，AI 不需主动维护其状态切换。
- 仅在用户**主动询问**"如何打印交付 / 如何走线下签字"时，再展开签字流程说明。

**与其他规则的关系**：
- 本规则覆盖 `项目与模块启动 SKILL §2.3 末"对齐决策段生成时强制约束"`、`§2.4 PRD 前置条件中的"对齐决策段已完成且 §9 已确认决策表已业务方口头确认"`等所有"签字门槛"措辞。
- 相关 SKILL / 规则文件原文保留不动，作为真实世界完整工作流参考。
- 若用户当轮明确要求"按 SKILL 严格签字门槛执行"，以用户当轮指令为准。

## 原型修改回写策略

**背景**：原型迭代频繁，业务变化必须回到权威文档，但纯实现修复不应污染 PRD、SRS 或 UI 规则。待回写机制只记录尚未同步的需求或可复用规则变化，并把实际回写集中到明确窗口。

**登记准入与目标分流（强制）**：

| 改动性质 | 是否登记 | 目标权威源 |
|---|---|---|
| 双速轨快速轨期间的草稿改动（尚无正式 PRD/SRS） | 否（不登记 RB） | 记入轻量需求摘要 §8 迭代记录（FB-XXX） |
| 新增、删除或改变业务能力、范围、角色权限、业务流程、业务规则 | 是 | 模块 PRD；涉及项目级范围时同时指向 P07 对应章节 |
| 字段、控件、校验、状态机、处理逻辑、异常反馈、接口或技术 AC 变化 | 是 | 模块 SRS |
| 可跨页面、跨模块复用的布局或交互约定 | 是 | 对应 `.cursor/rules/` 场景规则 |
| 按既有 PRD / SRS 完成功能实现，外部行为未变化 | 否 | 已有文档已覆盖 |
| Bug 修复、一次性 CSS / 像素调整、代码重构或依赖升级，外部行为未变化 | 否 | 代码与 Git 记录 |

- 同一改动同时影响业务语义和技术规格时，可在一条 RB 中列出多个目标锚点，但每项信息只写入其唯一权威源。
- 摘要只记录"需求或规则变化后的结果 + 原文档缺口"，禁止记录组件名、CSS 选择器、逐步修复过程等实现流水账。
- 无法确定是业务变化还是实现修复时，先核对 PRD / SRS；仍无法判断则向用户确认，不得默认登记到 UI 规则。

**默认行为（已通过准入时）**：
- 对原型代码（Vue 组件、mock 数据、UI 文案、交互流程）的修改，**AI 不主动修改任何已生成的需求文档**。
- AI 必须在代码端与持久化清单上执行三项动作：
 1. 在改动点附近以注释形式标注变更来源（例如 `// V0.4 修订：截止字段合并 - M4-Q-005`），便于后续定位。
 2. 在 `docs/_待回写清单.md` 追加 `RB-{nnn}`，状态为 `⏳ 待回写`，并准确填写目标权威源。
 3. 在当轮回复结尾以「**待回写提示**」复述编号、目标文档锚点和一句话结果；实际文档更新集中到下方回写窗口。

**强制回写触发条件（命中任一即统一批量回写已积累的"待回写清单"）**：

| # | 触发条件 |
|---|---|
| 1 | 用户调用 `/严格审查` SKILL 时 |
| 2 | 用户调用 `/交付检查` SKILL 时 |
| 3 | 用户调用 `/文档生成与版本管理` SKILL 时 |
| 4 | 用户调用 `/原型评审` SKILL 时 |
| 5 | 用户调用 `/需求变更` SKILL 时 |
| 6 | 用户调用 `/开发交接` SKILL 时 |
| 7 | 用户调用 `/接口对齐` SKILL 时 |
| 8 | 模块阶段切换时（如 M4 收尾切到 M5 之前，把 M4 文档统一落地）|
| 9 | 业务方 / 外部评审前（用户明确说"给业务方看 / 打印 / 正式交付"等；独立手动调用验收技能除外）|
| 10 | 项目阶段性里程碑或归档时（如 V1.0 / 一期收尾）|
| 11 | 用户**显式要求**"回写文档 / 同步到文档 / 更新文档"等措辞时 |

**冲突兜底**：
- 若某次代码改动会让已有 SRS-AC / 字段表 / Q-XXX 决策出现**语义矛盾**（不是细化，而是相反方向），AI 必须**立即停下**请求用户确认：①立刻回写 ②挂起到下一次触发窗口 ③回滚代码改动。不得在矛盾未澄清的情况下继续叠加新改动。
- 若 `docs/_待回写清单.md` 中 ⏳ 条目数 ≥ 10，AI 应主动提示用户「建议提前安排一次回写窗口」，但仍不主动写入业务文档；提示后等待用户决策是否提前触发回写窗口。

**持久化落盘位置（强制单一权威源）**：
- `docs/_待回写清单.md` 是本规则唯一允许的"待回写"持久化清单文件，只保留 `⏳ 待回写`与 `🟡 已推迟`条目、编号水位和历史归档索引。
- AI 不得在其他任何位置（如临时 md / 评论 / 代码注释中拼凑等效清单）维护"全局待回写"列表；`docs/archive/待回写记录-{YYYY}.md` 仅保存终态历史，不得出现 `⏳ 待回写`条目。
- 新增 RB 条目时，必须使用活动清单中的"下一个编号"，同步更新"最新已用编号"与"下一个编号"；编号归档后永久不得复用。
- 触发回写窗口时，AI 必须**先全文读取**活动清单，按 `⏳`状态逐项回写到目标文档锚点。
- 条目变为 `✅ 已回写`或`❌ 取消回写`后，必须追加到当年归档并从活动清单移除；`🟡 已推迟`继续留在活动清单并注明原因和重启条件。
- 当年归档不存在时按需创建；活动清单不得维护重复的历史回写汇总。

**与其他规则的关系**：
- 本规则**不覆盖**「严格审查员」三问自查的范围——AI 在交付时仍需在对话内部做"与文档是否一致"的检查，只是把"修复动作"延后到回写窗口执行。
- 本规则**不影响**新建文档的生成（如首次生成 SRS、首次生成模块 PRD），仅约束"对已生成文档的回写更新"。
- `项目验收文档` 是项目建设流程外的独立派生文档生成动作，不触发本节批量回写；如用户同时明确要求“同步/更新建设文档”，再按第 11 项进入回写窗口。
