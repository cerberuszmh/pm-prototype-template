# 待回写历史记录（2026）

> 本文件仅保存已完成、已取消等终态记录，不维护任何 `⏳ 待回写`事项。
> 当前待办的唯一权威源仍为 `docs/_待回写清单.md`；本归档只用于追溯，禁止在此新增待回写项。

## 终态条目

| 编号 | 状态 | 新增日期 | 来源（代码改动 / 原型场景） | 目标文档锚点（文件 + 章节） | 摘要（一句话改动 + 锚点偏离原因） |
|---|---|---|---|---|---|
| RB-001 | ✅ 已回写 | 2026-07-22 | `src/layouts/WorkspaceLayout.vue`、`src/styles/global.css`、`src/mock/example.js` 工作台滚动验证 | `.cursor/rules/vue-ui-implementation.mdc`「多标签页导航（PageTabs）」与「侧边栏」 | Header、标签栏与面包屑改为统一吸顶区域；桌面端左侧菜单与右侧内容分别独立滚动，窄屏恢复自然页面滚动。16 个验证菜单和 20 条验证记录完成实测后已移除，吸顶与滚动规则继续保留。 |
| RB-002 | ✅ 已回写 | 2026-07-22 | `src/views/example/ExampleListView.vue` 列表卡片 | `.cursor/rules/vue-ui-implementation.mdc`「列表页标题（卡片头）」 | 列表卡片 Body 使用 `11px 10px 10px` 内边距；顶部 11px 补偿 Header 与 Body 的 1px 边框重叠，使 Header 到表格及左右两侧的实测间距均为 10px。规则与示例实现已统一。 |
| RB-003 | ✅ 已回写 | 2026-07-22 | `src/views/example/ExampleListView.vue` 弹窗底部操作区 | `.cursor/rules/vue-ui-implementation.mdc`「全屏弹窗模式（Modal as Page）→ 底部按钮布局规则」 | 所有右侧操作统一使用 `.modal-footer__right` 并通过 `margin-left:auto` 靠右；含危险操作时增加 `.modal-footer__left`。规则、公共样式与示例实现已统一。 |
| RB-004 | ✅ 已回写 | 2026-07-22 | `src/views/example/ExampleListView.vue`、`src/styles/global.css` 标准尺寸修改弹窗 | `.cursor/rules/vue-ui-implementation.mdc`「弹窗尺寸自动选择」 | 已按内容数量、信息丰富度和交互复杂度建立标准、近全屏、完全全屏三级自动选择规则；标准尺寸为 `1200×600px`，小视口按四周 24px 安全边距收缩，完整框体结构与示例实现已统一。 |
| RB-005 | ✅ 已回写 | 2026-07-22 | `src/views/example/ExampleListView.vue` 表格多选与批量操作 | `.cursor/rules/vue-ui-implementation.mdc`「下拉与批量操作」 | 表格首列使用受控 Checkbox 多选，归档终态禁止选择；无选中项时批量按钮禁用，主按钮批量发布，下拉危险操作批量归档并二次确认，完成后清空选择。选中至少一条时，分页区域左侧显示「取消选择」按钮，点击后清空并隐藏；按钮文案规范已登记该选择状态清理例外。规则与示例实现已统一。 |
| RB-006 | ✅ 已回写 | 2026-07-22 | `src/layouts/WorkspaceLayout.vue` 顶部用户操作区 | `.cursor/rules/vue-ui-implementation.mdc`「用户头像与顶部操作区」 | 管理员入口右侧新增带 `LogoutOutlined` 前置图标的「退出登录」按钮，并与用户下拉菜单的退出项复用同一处理函数和反馈文案；退出后跳转 `/login`。规则与示例实现已统一。 |
| RB-007 | ✅ 已回写 | 2026-07-22 | `src/views/LoginView.vue`、`src/router/index.js` 模板登录页 | `.cursor/rules/vue-ui-implementation.mdc`「登录页」 | 新增独立 `/login` 登录页：系统名称为「示例管理系统」，桌面字号 `48px`，不展示 Logo，使用 Ant Design 表单；背景渐变由 `#48c6ef 0%` 到 `#6f86d6 100%` 调整为 `linear-gradient(60deg, #3d3393 0%, #2b76b9 37%, #2cacd1 65%, #35eb93 100%)`，并保持 `background-size: 220% 220%` 与 18 秒 `loginGradientShift` 位移动效及 6 秒柔光呼吸，减少动态效果时自动停用动画。登录容器由 `min-height: 100vh` 改为 `height: 100vh` 并保留 `overflow: hidden`，避免 body 出现右侧滚动条。桌面登录卡参考尺寸 `420×500px`、输入框 `44px`、相邻输入框间距 `34px`、验证码到登录按钮间距 `42px`、按钮 `46px`；验证码展示框与左侧输入框顶部对齐。包含验证码与温馨提示，不展示左侧装饰图形、左下角宣传文字及登录框右上模板标签。默认预填账号 `admin`、密码 `123456`，验证码不参与校验，可直接登录。 |
| RB-008 | ❌ 取消回写 | 2026-07-22 | `src/views/LoginView.vue`、`src/styles/global.css` 登录成功反馈、登录页滚动条修复 | — | 登录成功欢迎通知属于模板一次性反馈，滚动条处理属于 Bug 修复；均不形成跨项目权威规则。 |
| RB-009 | ✅ 已回写 | 2026-07-22 | `src/layouts/WorkspaceLayout.vue` 退出登录交互 | `.cursor/rules/vue-layout-navigation.mdc`「顶部用户操作」 | 退出登录属于高影响操作，所有入口统一先二次确认，确认后再退出并返回登录页。 |
| RB-010 | ❌ 取消回写 | 2026-07-23 | `src/styles/global.css`、`src/layouts/PageTabs.vue` 顶部操作区与标签栏更多按钮右对齐 | — | 单次像素对齐调整由代码保留，不形成跨项目权威规则。 |
| RB-011 | ❌ 取消回写 | 2026-07-23 | `src/layouts/WorkspaceLayout.vue`、`src/layouts/NotificationPopover.vue`、`src/mock/notifications.js` 通知中心 | — | 通知中心属于可选业务能力，当前模板无正式 PRD/SRS；真实项目启用时须重新登记到对应 PRD 与 SRS。 |
| RB-012 | ✅ 已回写 | 2026-07-23 | `src/layouts/WorkspaceLayout.vue`、`src/styles/global.css` 侧边栏收起按钮 | `.cursor/rules/vue-layout-navigation.mdc`「全局布局」 | 桌面端侧边栏提供稳定可达的展开/收起入口，收起后内容区与侧边栏背景同步适配。 |
| RB-013 | ❌ 取消回写 | 2026-07-23 | `src/layouts/NotificationPopover.vue` 通知中心底部操作按钮样式 | — | 通知中心的局部视觉样式由组件代码保留，不形成跨项目权威规则。 |
| RB-014 | ❌ 取消回写 | 2026-07-23 | `src/layouts/WorkspaceLayout.vue`、`src/layouts/AlarmFab.vue`、`src/mock/alarms.js` 告警信息悬浮按钮 | — | 告警入口属于可选业务能力，当前模板无正式 PRD/SRS；真实项目启用时须重新登记到对应 PRD 与 SRS。 |
| RB-015 | ✅ 已回写 | 2026-07-23 | `src/layouts/WorkspaceLayout.vue`、`src/styles/global.css` 侧边栏品牌区占位图标 | `.cursor/rules/vue-layout-navigation.mdc`「全局布局」 | 品牌区展开时显示系统图标和名称，收起时仅保留居中图标并隐藏名称。 |
| RB-016 | ❌ 取消回写 | 2026-07-23 | `src/styles/global.css`、`src/layouts/WorkspaceLayout.vue` 工作台全局布局与滚动容器 | — | 滚动容器异常属于 Bug 修复；既有布局规则已经规定桌面端单视口和内容区独立滚动，无须重复记录实现过程。 |

## 回写窗口记录

| 归档日期 | 触发窗口 | 回写条目数 | 涉及目标文档 | 备注 |
|---|---|---:|---|---|
| 2026-07-22 | 用户显式要求更新样式规则 | 1 | `.cursor/rules/vue-ui-implementation.mdc` | RB-001：补充顶部导航整体吸顶、左右区域独立滚动及窄屏自然滚动规则；验证夹具已于同日移除。 |
| 2026-07-22 | 用户按建议修复样式冲突 | 1 | `.cursor/rules/vue-ui-implementation.mdc`、`src/styles/global.css` | RB-003：统一弹窗底部左右操作分区规则，并补齐左侧危险操作分区样式。 |
| 2026-07-22 | 用户显式要求回写待回写清单 | 1 | `.cursor/rules/vue-ui-implementation.mdc` | RB-002：补充带 Card Header 的列表卡片 Body 间距及 1px 边框补偿规则。 |
| 2026-07-22 | 用户显式要求更新弹窗样式规则 | 1 | `.cursor/rules/vue-ui-implementation.mdc` | RB-004：补充标准、近全屏、完全全屏三级自动选择条件及标准弹窗结构。 |
| 2026-07-22 | 用户显式要求更新批量操作样式规则 | 1 | `.cursor/rules/vue-ui-implementation.mdc` | RB-005：补充 Checkbox 行选择、批量操作状态联动及取消选择按钮定位规则。 |
| 2026-07-22 | 用户显式要求更新待回写 | 2 | `.cursor/rules/vue-ui-implementation.mdc` | RB-006：扩展「用户头像」为「用户头像与顶部操作区」，补充直接退出登录按钮规则；RB-007：新建「登录页」章节，写入独立路由、背景渐变、动效、容器高度及表单规格。 |
| 2026-07-23 | 用户显式要求收紧原型回写机制 | 3 | `.cursor/rules/vue-layout-navigation.mdc` | RB-009、RB-012、RB-015 回写结果性布局规则；RB-008、RB-010、RB-011、RB-013、RB-014、RB-016 按新准入标准取消回写。 |

## 编号水位

- 本归档最高编号：`RB-016`。
- 全项目编号不得复用；后续从 `RB-017` 开始。
