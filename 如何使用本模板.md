# 如何使用本模板

> 本文件是 PM 启动新项目时的**第一份阅读材料**，回答"拿到这个模板文件夹后，我该做哪些事才能开始正式干活"。
>
> 如果你想了解项目全流程（产品发现 → PRD → SRS → 原型 → 评审 → 交付），请阅读 `[项目全流程操作手册.md](./项目全流程操作手册.md)`。
>
> 本文件只讲"怎么用模板"，操作手册讲"项目流程怎么走"，两者分工互补。

---

## 0. 5 分钟开始

> 本节是首次使用的最短入口，只完成“可以正确开工”的准备。首次正式使用仍须按 §二、§三完成全部占位项检查。

### 0.1 先选择工作模式

- **纯文档模式**：使用 `.cursor/`、`templates/` 和 `docs/`；跳过依赖安装与 Vue 原型启动。
- **文档 + 原型模式**：除文档体系外，还要验证 `src/` 原型骨架能够运行。

### 0.2 五分钟动作

1. **第 0～1 分钟**：复制模板文件夹，确定项目正式全称和系统中文名；文档 + 原型模式同时确定项目英文短名。
2. **第 1～3 分钟**：按 §三“必须替换的占位项清单”完成当前模式对应的替换；不要修改 `.cursor/rules/` 和 `.cursor/skills/`。
3. **第 3～4 分钟**：
  - 纯文档模式：打开 `docs/samples/README.md`，确认模板、样例和正式交付物的区别。
  - 文档 + 原型模式：执行 `npm install`、`npm run dev`，确认“示例列表”可访问。
4. **第 4～5 分钟**：在 Cursor 输入 `/项目总入口`，或直接描述“我要启动一个新项目，请从产品发现开始”。



### 0.3 完成标准

- 已选定纯文档或文档 + 原型模式。
- 项目名称与系统名称不再保留模板占位文案。
- 原型模式下示例列表能够访问；纯文档模式明确跳过该项。
- 已从 `/项目总入口` 进入项目级启动分流，而不是直接跳到 SRS 或原型。

---



## 一、模板设计意图

本模板是一份**产品经理启动新项目即用的通用骨架**，已内置：

- **AI 治理规则**（`.cursor/rules/`）：7 份规则文件，约束 AI 在文档生成、原型实现、文件编辑、会话切换等环节的行为，自动生效。
- **工作流 Skills**（`.cursor/skills/`）：10 个项目级 SKILL，覆盖产品发现、项目与模块启动、评审、需求变更、开发交接、文档生成、交付检查、接口对齐等全流程。
- **文档模板**（`templates/`）：18 份正式交付文档模板（PRD、SRS、需求地图、JTBD、用户旅程、IA、流程图、数据字典、测试用例、用户手册、评审 checklist 等）。
- **最小可运行原型骨架**（`src/`）：Vue 3 + Vite + Ant Design Vue 4.x，含布局、多标签页、路由、示例模块，`npm install && npm run dev` 即可启动。
- **飞书工具集**（`.agents/skills/`）：10 个 lark-cli skill，支持飞书文档/表格/IM/知识库等操作，新项目开箱即用。

---



## 二、首次使用步骤



### 步骤 1：复制模板文件夹

将本模板文件夹整体复制一份，重命名为新项目名，例如：

```
d:\工作\我的工作\{新项目名}\
```



### 步骤 2：替换占位项

打开新项目文件夹，按当前工作模式替换占位内容（**必须**在正式启动产品发现之前完成，否则 AI 会把占位文案写进正式文档）：


| #   | 文件                                               | 占位内容                                  | 替换为                                      | 适用模式    |
| --- | ------------------------------------------------ | ------------------------------------- | ---------------------------------------- | ------- |
| 1   | `package.json`                                   | `"name": "pm-prototype-template"`     | 新项目英文短名，如 `"abbvie-registration-system"` | 文档 + 原型 |
| 2   | `src/layouts/WorkspaceLayout.vue`                | `const systemName = "系统名称（启动时替换）"`    | 新项目正式中文名，与项目级 PRD §1 一致                  | 文档 + 原型 |
| 3   | `src/views/HomeView.vue`                         | `title="PM 初始化项目模板"` 与 `sub-title` 文案 | 新项目首页欢迎文案                                | 文档 + 原型 |
| 4   | `index.html`                                     | `<title>...</title>`                  | 新项目浏览器标签页标题                              | 文档 + 原型 |
| 5   | `templates/requirements/srs-template.md`         | `{项目全称}`、`{系统名}`                      | 新项目完整全称、系统中文名                            | 两种模式    |
| 6   | `templates/requirements/project-prd-template.md` | `{项目全称}`、`{系统名}`                      | 新项目完整全称、系统中文名                            | 两种模式    |
| 7   | `templates/requirements/module-prd-template.md`  | `{项目全称}`、`{系统名}`                      | 新项目完整全称、系统中文名                            | 两种模式    |




### 步骤 3：安装依赖并启动

本步骤仅适用于“文档 + 原型”模式；纯文档模式直接跳到步骤 4。

```bash
npm install
npm run dev
```

启动后访问 `http://localhost:5173/`，确认：

- 首页正常渲染
- 左侧菜单含"系统首页 + 示例模块"两项
- 点击"示例列表"能进入示例页面，新建/编辑弹窗、状态切换、分页均正常



### 步骤 4：在 Cursor 中正式启动项目

在 Cursor 对话框中输入：

```
/项目总入口
```

或直接描述你的项目目标，例如：

```
我要启动一个新项目，叫做「XX 系统」，主要用户是 YY，核心场景是 ZZ，请帮我从产品发现开始走起。
```

AI 会按 `项目总入口` SKILL 自动分流到 `产品发现` → `项目与模块启动` 流程，产出系统级 6 件套与项目级 PRD 骨架版。

---



## 三、必须替换的占位项清单（速查）

下表汇总需要人工替换的占位项，可在步骤 2 中按模式勾对：

**两种模式均须完成**

- [ ] `templates/requirements/srs-template.md` 的项目全称与系统名占位
- [ ] `templates/requirements/project-prd-template.md` 的项目全称与系统名占位
- [ ] `templates/requirements/module-prd-template.md` 的项目全称与系统名占位

**仅文档 + 原型模式须完成**

- [ ] `package.json` 的 `name` 字段
- [ ] `src/layouts/WorkspaceLayout.vue` 的 `systemName` 常量
- [ ] `src/views/HomeView.vue` 的 `title` 与 `sub-title`
- [ ] `index.html` 的 `<title>`

> 其他文件（`.cursor/rules/`、`.cursor/skills/`、`templates/` 多数模板、`docs/` 骨架）均为通用内容，无需替换。

---



## 四、通用资产 vs 业务资产边界

理解这条边界，才能知道"哪些是模板自带的，哪些是新项目自己产出的"。

### 通用资产（模板自带，新项目继承）


| 目录                                                                | 内容                | 新项目是否改动              |
| ----------------------------------------------------------------- | ----------------- | -------------------- |
| `.cursor/rules/`                                                  | 7 份 AI 治理规则       | 一般不改；如需调整规则，走规则变更流程  |
| `.cursor/skills/`                                                 | 10 个项目级 SKILL     | 一般不改；SKILL 体系升级时统一更新 |
| `.agents/skills/`                                                 | 10 个飞书 lark skill | 不改；lark-cli 升级时统一更新  |
| `templates/`                                                      | 18 份文档模板          | 仅替换占位项；模板结构不动        |
| `scripts/`                                                        | 死引用检查脚本           | 不动                   |
| `docs/samples/`                                                   | PM 脱敏成品样例包        | 只读对照；真实项目不得直接改名交付    |
| `src/layouts/`、`src/composables/`、`src/styles/`                   | 布局/标签页/全局样式       | 一般不动；新增页面时复用         |
| `src/views/HomeView.vue`、`PlaceholderView.vue`、`NotFoundView.vue` | 系统首页/占位页/404      | 仅替换首页文案              |
| `src/router/index.js`、`src/layouts/WorkspaceLayout.vue`           | 路由表与侧边栏           | 每新增一个模块追加一项          |




### 业务资产（新项目自己产出，模板只提供空骨架）


| 目录                                                                         | 内容                                    | 产出方式                        |
| -------------------------------------------------------------------------- | ------------------------------------- | --------------------------- |
| `docs/discovery/`                                                          | 产品发现 6 件套（P01~P06）                    | 触发 `/产品发现` SKILL            |
| `docs/formal-requirements/`                                                | 项目级 PRD（P07）+ 模块级 PRD/SRS（M{n}-02/03） | 触发 `/项目与模块启动`               |
| `docs/review/`                                                             | 严格审查报告、原型评审结论                         | 触发 `/评审` SKILL              |
| `docs/flow/`                                                               | 业务流程说明                                | 触发 `/文档生成与版本管理`             |
| `docs/data/`                                                               | 数据字典                                  | 触发 `/开发交接` SKILL            |
| `docs/api-alignment/`                                                      | 接口对齐报告                                | 触发 `/接口对齐` SKILL            |
| `docs/changes/`                                                            | 需求变更记录                                | 触发 `/需求变更` SKILL            |
| `docs/testing/`                                                            | 测试用例                                  | 触发 `/文档生成与版本管理`             |
| `docs/manuals/`                                                            | 用户手册                                  | 触发 `/文档生成与版本管理`             |
| `docs/user-requirements/`                                                  | 用户原始需求材料、待确认问题清单                      | 手工归档或 SKILL 产出              |
| `docs/_待回写清单.md`、`docs/archive/待回写记录-{YYYY}.md`                        | 原型小改活动待办与年度终态归档                       | AI 自动维护，触发回写窗口时批量回写并归档     |
| `src/views/{模块目录}/`                                                        | 各功能模块页面                               | 按模块逐个新增；开发交接时作为原型代码包强制项     |
| `src/mock/{模块目录}.js`                                                       | 默认扁平 Mock 文件                          | 按模块逐个新增；复杂模块可改用嵌套目录，简单单页可内联 |
| `src/mock/enums.js`                                                        | 共享业务枚举                                | 清空示例枚举后按模块新增；交接时只纳入实际引用项    |
| `src/router/index.js`、`src/layouts/WorkspaceLayout.vue`、`src/composables/` | 路由、菜单与复用逻辑                            | 被模块使用时纳入原型代码包依赖清单           |


> **原型代码包规则**：页面组件必须交付；Mock、路由、菜单、共享枚举和 composable 按实际引用“存在则纳入”。评审与开发交接统一按 `vue-ui-implementation.mdc §"Mock 组织与发现协议"` 检索扁平、嵌套、共享枚举和内联 Mock。



### 示例资产（模板自带，新项目启动时清空或替换）


| 文件                                      | 内容                              | 处理方式                                       |
| --------------------------------------- | ------------------------------- | ------------------------------------------ |
| `src/views/example/ExampleListView.vue` | 示例列表页（标准列表页参照样板）                | 新项目首个模块开发时，可复制改造为首个业务页面，随后删除 example 目录    |
| `src/mock/example.js`                   | 示例 mock 数据                      | 同上，改造后删除                                   |
| `src/mock/enums.js` 中的 `EXAMPLE_STATUS` | 示例状态枚举                          | 清空，替换为首个业务模块的真实枚举                          |
| `docs/samples/`                         | 与示例页对应的 P07、模块 PRD/SRS 和两阶段评审成品 | 从 `docs/samples/README.md` 按顺序阅读；只用于学习填写深度 |


---



## 五、与"项目全流程操作手册.md"的分工


| 文件                  | 回答的问题                                                     | 阅读时机             |
| ------------------- | --------------------------------------------------------- | ---------------- |
| **本文件**（如何使用本模板.md） | 拿到模板文件夹后，第一件事做什么？占位项有哪些？哪些是通用资产、哪些是业务资产？                  | 首次使用模板时阅读一次      |
| **项目全流程操作手册.md**    | 一个新项目从启动到 PM 开发交接要走哪些阶段？每阶段的输入输出是什么？由哪个 SKILL 承载？产物归档到哪里？ | 每次启动新项目、或忘记流程时翻阅 |


两份文件互不重复：

- 本文件讲"模板怎么用"（一次性Setup）
- 操作手册讲"项目怎么走"（全流程Stages）

---



## 六、常见问题

**Q1：为什么** `docs/` **的业务交付目录都是空的** `.gitkeep`**？**
A：业务交付目录只提供空骨架，正式文档由各 SKILL 在流程中产出；`.gitkeep` 保证空目录能被 git 跟踪。`docs/samples/` 是唯一例外，其中保存虚构脱敏成品，帮助 PM 对照学习，不属于真实项目交付物。

**Q2：**`docs/_待回写清单.md` **为什么是空的？**
A：这是 `project-collaboration.mdc` §"原型修改回写策略"的活动清单。新项目启动后，AI 在原型小改发生时自动追加 RB-XXX 条目；触发回写窗口（如 `/评审` 阶段 1、`/交付检查`）时批量回写，并将已回写或已取消记录移入年度归档。

**Q3：**`src/views/example/` **这个示例模块要保留多久？**
A：它是"标准列表页参照样板"，建议保留到首个真实业务模块开发完成。新人可参照它的结构（筛选卡 + 列表卡 + 全屏弹窗 + 分页 + 状态 Tag + Toast 文案）快速上手。首个业务模块完工后可删除。

**Q4：**`.agents/skills/` **这么多飞书工具，我不用飞书怎么办？**
A：不影响。`.agents/skills/` 是 lark-cli 的工具集，仅在用户主动调用飞书相关操作时才被加载。不用飞书的项目可整目录删除，不影响 `.cursor/` 治理体系与 SKILL 工作流。

**Q5：可以不装 Node.js 直接用模板的文档体系吗？**
A：可以。`.cursor/` + `templates/` + `docs/` 构成的文档治理体系不依赖 Node.js。只有 `src/` 原型骨架需要 Node.js 运行。如果你只做需求文档不做原型，可删除 `src/`、`package.json`、`vite.config.js`、`index.html`。

**Q6：新项目启动后，AI 怎么知道用这套规则？**
A：Cursor 自动加载 `.cursor/rules/` 下的所有 `.mdc` 文件作为常驻规则。SKILL 在用户触发关键词（如 `/项目与模块启动`、`/评审`）时被调用。无需手工配置。

---



## 七、下一步

完成步骤 1~4 后，你已经具备：

- ✅ 一份干净的新项目文件夹
- ✅ AI 治理规则已生效
- ✅ SKILL 工作流已就绪
- ✅ 原型骨架可运行

接下来请阅读 `[项目全流程操作手册.md](./项目全流程操作手册.md)`，按第一部分"项目级首次启动"开始走产品发现流程。